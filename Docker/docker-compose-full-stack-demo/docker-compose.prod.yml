version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./env/prod.env
    expose:
      - "3000"      # ❌ 不对外暴露

  frontend:
    build: ./frontend
    ports:
      - "80:80"     # ✅ 唯一入口
    depends_on:
      - backend
