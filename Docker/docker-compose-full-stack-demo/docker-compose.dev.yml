version: "3.9"

services:
  backend:
    build: ./backend
    env_file:
      - ./env/dev.env
    ports:
      - "3000:3000"   # ✅ 直接暴露，方便 curl / Postman

  frontend:
    build: ./frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
